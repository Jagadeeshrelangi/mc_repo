# Pinned to the versions installed in the verified dev environment
# (backend/venv, freeze 2026-08-06). Do not use ">=" — upgrades must be
# deliberate and re-tested.
fastapi==0.139.0
uvicorn==0.49.0
pydantic==2.13.4
pydantic-settings==2.14.2
pandas==3.0.3
numpy==2.5.0
scikit-learn==1.9.0
xgboost==3.3.0
joblib==1.5.3
python-multipart==0.0.32
python-dotenv==1.2.2
requests==2.34.2
firebase-admin==7.5.0
langchain==1.3.11
langchain-community==0.4.2
langchain-google-genai==4.2.6
faiss-cpu==1.14.3
sentence-transformers==5.6.0
pypdf==6.14.2
python-docx==1.2.0

# Database foundation (Sprint 2, Task 2) — pinned to backend/venv
sqlalchemy==2.0.51
greenlet==3.5.3
asyncpg==0.31.0
alembic==1.19.1

# Authentication foundation (Sprint 2, Task 3, Stage 1) — pinned to backend/venv.
# bcrypt is pinned to 4.0.1 for passlib 1.7.4 compatibility on Python 3.13:
# bcrypt>=4.1 rejects >72-byte secrets, which breaks passlib's backend probe.
python-jose[cryptography]==3.5.0
passlib==1.7.4
bcrypt==4.0.1
